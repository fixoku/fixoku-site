import assert from "node:assert/strict"; import {loadLocalEnv} from "./local-env.mjs"; loadLocalEnv(); const origin=process.env.APP_ORIGIN||"http://127.0.0.1:5173",password=process.env.TEST_SEED_PASSWORD; let cookie=""; async function req(path,opt={}){const h=new Headers(opt.headers||{});h.set("origin",origin);if(cookie)h.set("cookie",cookie);const r=await fetch(origin+path,{...opt,headers:h});const c=r.headers.get("set-cookie");if(c)cookie=c.split(";")[0];return {r,b:await r.json().catch(()=>({}))}} let x=await req("/api/auth/sign-in/email",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email:"trainer.phase1c@example.test",password})});assert.equal(x.r.status,200);x=await req("/api/trainer/students");assert.equal(x.r.status,200);assert.ok(x.b.students.length>=1);const id=x.b.students[0].id;x=await req(`/api/trainer/students/${id}`);assert.equal(x.r.status,200);x=await req(`/api/trainer/students/${id}?trainerId=other`);assert.equal(x.r.status,200);console.log(JSON.stringify({canonicalStudentsApi:"PASS",assignedRoster:"PASS",detail:"PASS",queryOverrideIgnored:"PASS"}));
