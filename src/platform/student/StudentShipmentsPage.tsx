import { useEffect,useState } from "react";
import { StudentPanelShell } from "./StudentPanelShell";
import "./student-portal.css";
const labels:Record<string,string>={WAITING:"Bekliyor",PREPARING:"Hazırlanıyor",PACKED:"Paketlendi",SHIPPED:"Kargoya verildi",DELIVERED:"Teslim edildi",RETURNED:"İade edildi",CANCELLED:"İptal edildi"};
const carrierLabel=(value?:string|null)=>!value||value==="LOCAL_TEST"?"Sendeo":value;
export function StudentShipmentsPage({user={name:"Öğrenci"}}:{user?:{name:string}}){const [rows,setRows]=useState<any[]>([]);const [state,setState]=useState("loading");useEffect(()=>{fetch("/api/student/shipments",{credentials:"same-origin"}).then(async r=>{if(!r.ok)throw Error();setRows((await r.json()).shipments||[]);setState("ready")}).catch(()=>setState("error"))},[]);return <StudentPanelShell user={user}><div className="student-portal__hero"><div><h1>Kargolarım</h1><p>Fiziksel ürün teslimatlarının güncel durumunu takip edin.</p></div></div>{state==="loading"&&<div className="student-portal__state" role="status">Kargolar yükleniyor…</div>}{state==="error"&&<div className="student-portal__state" role="alert"><h3>Kargo bilgileri yüklenemedi</h3><p>Lütfen daha sonra tekrar deneyin.</p></div>}{state==="ready"&&(!rows.length?<div className="student-portal__state"><h3>Henüz gönderi yok</h3><p>Henüz kargoya verilmiş fiziksel ürününüz bulunmuyor.</p></div>:<section className="student-shipment-list" aria-label="Kargo gönderileri">{rows.map(r=><article className="student-portal__card student-shipment-card" key={r.id}><div><span className="student-portal__status">{labels[r.status]||"Durum bekleniyor"}</span><h2>{r.orderId?`Sipariş ${String(r.orderId).slice(0,8)}`:"Gönderi"}</h2></div><dl><div><dt>Ürün</dt><dd>{(r.items||[]).map((i:any)=>`${i.titleSnapshot} × ${i.quantity}`).join(", ")||"Fiziksel ürün"}</dd></div><div><dt>Kargo firması</dt><dd>{carrierLabel(r.carrier)}</dd></div><div><dt>Takip numarası</dt><dd>{r.trackingNumber||"Henüz yok"}</dd></div><div><dt>Tarih</dt><dd>{r.shippedAt?new Intl.DateTimeFormat("tr-TR",{dateStyle:"medium"}).format(new Date(r.shippedAt)):"Hazırlanıyor"}</dd></div></dl></article>)}</section>)}</StudentPanelShell>}
