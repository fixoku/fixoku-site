import { useCallback, useEffect, useMemo, useState } from "react";

import "./student-portal.css";

type User={id:string;name:string;email:string};
const formatDisplayName=(value:string)=>value.trim().split(/\s+/u).map((part)=>{const lower=part.toLocaleLowerCase("tr-TR");return lower.charAt(0).toLocaleUpperCase("tr-TR")+lower.slice(1)}).join(" "); type Context={studentProfileId:string;student:{name:string;grade:string|null;school:string|null;status:string}}; type Enrollment={id:string;status:string;program:{title:string;slug:string;shortDescription?:string};trainerNames?:string[]}; type Body={role:"STUDENT"|"GUARDIAN";context:Context|null;students:{studentProfileId:string;name:string}[];enrollments:Enrollment[]};

export function StudentPortalPage({user,roles}:{user:User;roles:string[]}){const isGuardian=roles.includes("GUARDIAN")&&!roles.includes("STUDENT"); const [body,setBody]=useState<Body|null>(null); const [state,setState]=useState<"loading"|"ready"|"error">("loading"); const [selected,setSelected]=useState(""); const load=useCallback(async(id?:string)=>{setState("loading");try{const query=id?`?studentProfileId=${encodeURIComponent(id)}`:"";const r=await fetch(`/api/student/portal-context${query}`,{credentials:"same-origin"});if(!r.ok)throw new Error();const next=await r.json() as Body;setBody(next);setSelected(next.context?.studentProfileId??"");setState("ready")}catch{setState("error")}},[]);useEffect(()=>{void load()},[load]);const initials=useMemo(()=>body?.context?.student.name.split(/\s+/).map((x)=>x[0]).join("").slice(0,2).toLocaleUpperCase("tr-TR")||"Ö",[body]); if(state==="loading")return <main className="student-portal"><div className="student-portal__state" role="status" aria-live="polite">Öğrenci paneli yükleniyor…</div></main>; if(state==="error")return <main className="student-portal"><div className="student-portal__state" role="alert"><h3>Panel verileri yüklenemedi</h3><p>Bilgilerinize şu anda ulaşılamıyor.</p><button className="student-portal__retry" type="button" onClick={()=>void load(selected||undefined)}>Tekrar dene</button></div></main>; const context=body?.context; const students=body?.students ?? []; const enrollments=body?.enrollments ?? []; return <div className="student-portal-shell"><aside className="student-portal__sidebar" aria-label="Öğrenci paneli menüsü"><div className="student-portal__brand"><img src="/logo-fixoku.png" alt="Fixoku" /></div><nav aria-label="Öğrenci navigasyonu"><a className="student-portal__nav-item is-active" href="/panel/ogrenci">Ana Sayfa</a></nav><div className="student-portal__slogan">Daha<br />aydınlık nesiller<br />için...</div></aside><main className="student-portal"><header className="student-portal__topbar"><div><strong>Öğrenci Paneli</strong><span>{isGuardian?"Veli görünümü":"Öğrenci görünümü"}</span></div><div className="student-portal__account"><span>{formatDisplayName(user.name)}</span><button type="button" onClick={()=>{void fetch("/api/auth/sign-out",{method:"POST",credentials:"same-origin"}).finally(()=>{window.location.assign("/giris")})}}>Çıkış</button></div></header><p className="student-portal__crumb">Ana Sayfa <span aria-hidden="true">›</span> Öğrenci Paneli</p><div className="student-portal__hero"><div><h2>{isGuardian?"Veli görünümü":"Öğrenci Paneli"}</h2><p>{isGuardian?"Bağlı öğrencinin eğitim bilgilerini buradan takip edebilirsiniz.":`Merhaba, ${formatDisplayName(user.name)}. Eğitimlerine buradan ulaşabilirsin.`}</p></div>{isGuardian&&students.length>0&&<div className="student-portal__context"><small>Görüntülenen öğrenci</small><label htmlFor="student-context-select" className="student-portal__sr-only">Öğrenci seçin</label><select id="student-context-select" value={selected} onChange={(e)=>{setSelected(e.target.value);void load(e.target.value)}}>{students.map((s)=><option key={s.studentProfileId} value={s.studentProfileId}>{formatDisplayName(s.name)}</option>)}</select></div>}</div>{isGuardian&&context&&<div className="student-portal__banner" role="status">Şu anda {formatDisplayName(context.student.name)}’in eğitim bilgilerini görüntülüyorsunuz.</div>}{!context?<div className="student-portal__state"><h3>{isGuardian?"Bağlı öğrenci bulunamadı":"Profiliniz henüz hazır değil"}</h3><p>{isGuardian?"Yetkilendirilmiş bir öğrenci ilişkisi tanımlandığında burada görünecek.":"Eğitim bilgileriniz hazır olduğunda bu alanda görüntülenecek."}</p></div>:<div className="student-portal__grid"><section className="student-portal__card" aria-labelledby="student-identity-title"><h3 id="student-identity-title">Öğrenci bilgileri</h3><div className="student-portal__identity"><div className="student-portal__avatar" aria-hidden="true">{initials}</div><div><strong>{formatDisplayName(context.student.name)}</strong><p>{context.student.grade||"Seviye belirtilmedi"}{context.student.school?` · ${context.student.school}`:""}</p></div></div></section><section className="student-portal__card" aria-labelledby="student-access-title"><h3 id="student-access-title">{isGuardian?"Eğitmeni":"Eğitmenim"}</h3>{enrollments[0]?.trainerNames?.length ? <><strong className="student-portal__summary-trainer">{enrollments[0].trainerNames.map(formatDisplayName).join(", ")}</strong><p>{isGuardian?"Seçili öğrencinin eğitimine eşlik eden eğitmen.":"Eğitimin boyunca sana eşlik eden eğitmen."}</p></> : <p>Eğitmen ataması henüz yapılmadı.</p>}</section><section className="student-portal__card student-portal__enrollments" aria-labelledby="enrollment-title"><h3 id="enrollment-title">Eğitimlerim</h3>{enrollments.length?enrollments.map((e)=><div className="student-portal__enrollment" key={e.id}><div><strong>{e.program.title}</strong><span>{e.program.shortDescription || "Fixoku eğitim programı"}</span>{e.trainerNames?.length ? <span className="student-portal__trainer">Eğitmenim: {e.trainerNames.map(formatDisplayName).join(", ")}</span> : <span className="student-portal__trainer">Eğitmen ataması henüz yapılmadı.</span>}</div><span className="student-portal__status">{e.status==="ACTIVE"?"Aktif":e.status}</span></div>):<p>Henüz aktif bir eğitim kaydınız bulunmuyor.</p>}</section></div>}</main></div>}
