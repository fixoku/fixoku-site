import assert from "node:assert/strict"; import pg from "pg"; import { loadLocalEnv } from "./local-env.mjs"; loadLocalEnv(); const reset=new pg.Pool({connectionString:process.env.DATABASE_URL}); await reset.query("delete from rate_limit"); await reset.end(); const origin=process.env.APP_ORIGIN||"http://127.0.0.1:5173"; const roles={TRAINER:"/panel/egitmen",STUDENT:"/panel/ogrenci",GUARDIAN:"/panel/ogrenci",SUPER_ADMIN:"/panel/admin"}; for(const [role,dest] of Object.entries(roles)){const r=await fetch(origin+"/api/dev-login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({role})});assert.equal(r.status,200);assert.deepEqual((await r.json()).redirect,dest);assert.ok(r.headers.get("set-cookie"));} let r=await fetch(origin+"/api/dev-login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({role:"UNKNOWN"})});assert.equal(r.status,400);r=await fetch(origin+"/api/dev-login");assert.equal(r.status,405);console.log(JSON.stringify({devLogin:"PASS",trainer:"PASS",student:"PASS",guardian:"PASS",superAdmin:"PASS",unknownRole:"PASS",realBetterAuthSession:"PASS",passwordExposed:"NO"}));
