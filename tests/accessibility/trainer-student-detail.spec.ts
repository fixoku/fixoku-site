import { handleConsent, switchReviewUser } from "../support/consent";
import {test,expect} from "@playwright/test"; import AxeBuilder from "@axe-core/playwright"; import {readFileSync} from "node:fs";
test("trainer student detail accessibility",async({page})=>{const password=process.env.TEST_SEED_PASSWORD||readFileSync(".env.local","utf8").match(/^TEST_SEED_PASSWORD=(.*)$/m)?.[1];await switchReviewUser(page, "trainerWithStudents");await page.goto("/panel/egitmen/ogrencilerim");const link=page.getByRole("link",{name:/Detay/}).first();await link.click();await expect(page.getByRole("heading",{name:"Öğrenci Detayı"})).toBeVisible();const r=await new AxeBuilder({page}).analyze();const bad=r.violations.filter(v=>v.impact==="critical"||v.impact==="serious");console.log("TRAINER_STUDENT_DETAIL_A11Y="+JSON.stringify({critical:r.violations.filter(v=>v.impact==="critical").length,serious:bad.length}));expect(bad).toHaveLength(0);});
