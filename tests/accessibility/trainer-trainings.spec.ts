import { handleConsent, switchReviewUser } from "../support/consent";
import { test, expect } from "@playwright/test"; import AxeBuilder from "@axe-core/playwright"; import { readFileSync } from "node:fs";
test("trainer trainings accessibility scan",async({page})=>{const password=process.env.TEST_SEED_PASSWORD||readFileSync(".env.local","utf8").match(/^TEST_SEED_PASSWORD=(.*)$/m)?.[1];await switchReviewUser(page, "trainer");await page.goto("/panel/egitmen/egitimlerim");await expect(page.getByRole("heading",{name:"Eğitimlerim"})).toBeVisible();const result=await new AxeBuilder({page}).analyze();const bad=result.violations.filter(v=>v.impact==="serious"||v.impact==="critical");console.log("TRAINER_TRAININGS_A11Y="+JSON.stringify({critical:result.violations.filter(v=>v.impact==="critical").length,serious:bad.length}));expect(bad).toHaveLength(0);});
